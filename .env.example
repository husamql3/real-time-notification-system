# These will be automatically created by Neon Launchpad (https://neon.new).
DATABASE_URL=
DATABASE_URL_POOLER=

# Email address that has admin access to /admin (broadcast center)
# Matches the seeded admin user in db/init.sql
ADMIN_EMAIL=admin@example.com
